table:
  name: workflow_triggers
  schema: public
insert_permissions:
  - role: public
    permission:
      check: {}
      columns:
        - id
        - workflow_id
        - type
        - config
        - is_active
select_permissions:
  - role: public
    permission:
      columns:
        - id
        - workflow_id
        - type
        - config
        - is_active
        - created_at
      filter: {}
